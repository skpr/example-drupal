type: drupal

size: "small"

ingress:
  proxy:
    php:
      path: /php/*
      origin: dev.php.test.skpr.dev

cron:
  drush:
    command:  "drush cron"
    schedule: "@hourly"

metrics:
  enabled: true
